{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.6.0"
  },
  "dependencies": {
    "jest": "^29.6.4",
    "save-dev": "^0.0.1-security"
  }
}
